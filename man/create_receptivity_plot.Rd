% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_receptivity_plot.R
\name{create_receptivity_plot}
\alias{create_receptivity_plot}
\title{Criar gráfico de receptividade climática e temperatura mínima}
\usage{
create_receptivity_plot(api_data, weeks_limit)
}
\arguments{
\item{api_data}{Data frame contendo os dados climáticos. Deve incluir
ao menos as colunas \code{SE} (semana epidemiológica) e \code{tempmin}
(temperatura mínima).}

\item{weeks_limit}{Valor da última semana epidemiológica considerada
na análise.}
}
\value{
Um objeto \code{ggplot} contendo o gráfico de receptividade climática
baseado na temperatura mínima semanal.
}
\description{
Gera um gráfico de séries temporais da temperatura mínima semanal e
da receptividade climática para transmissão de arboviroses. O gráfico
destaca visualmente as semanas em que a temperatura mínima está acima
do limiar favorável à transmissão (18°C), utilizando faixas coloridas
no fundo do gráfico.
}
\details{
O gráfico também inclui uma linha horizontal indicando o limiar de
temperatura favorável à transmissão e apresenta a evolução semanal
da temperatura mínima.

O gráfico inclui:
\itemize{
\item Faixas coloridas indicando semanas com temperatura mínima
favorável (\verb{tempmin >= 18°C}) ou desfavorável.
\item Linha temporal da temperatura mínima semanal.
\item Linha horizontal indicando o limiar climático favorável à
transmissão de arboviroses (18°C).
}

As semanas epidemiológicas exibidas são limitadas à janela
de um ano anterior até a semana atual.
}
\examples{
\dontrun{
create_receptivity_plot(
  api_data = dados_api,
  weeks_limit = 202452
)
}

}
