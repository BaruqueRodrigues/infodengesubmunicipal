% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classificar_bairros_distrito.R
\name{classificar_bairros_distrito}
\alias{classificar_bairros_distrito}
\title{Classificar bairros em distritos por interseção espacial}
\usage{
classificar_bairros_distrito(sp_distritos, sp_bairros)
}
\arguments{
\item{sp_distritos}{Objeto \code{sf} contendo os polígonos dos distritos.
Deve possuir ao menos a coluna \code{distrito} e geometria poligonal.}

\item{sp_bairros}{Objeto \code{sf} contendo os polígonos dos bairros.
Deve possuir as colunas \code{nome_bairr}, \code{id_bairro} e geometria.}
}
\value{
Um objeto \code{sf} contendo os bairros com a coluna adicional:
\itemize{
\item \code{distrito} — distrito associado ao bairro
}

A geometria retornada corresponde à geometria original dos bairros
(renomeada para \code{geometry_distritos}).
}
\description{
Associa cada bairro a um distrito utilizando operações espaciais do
pacote \code{sf}. A função calcula o centróide dos polígonos de bairros e
identifica o distrito correspondente por interseção espacial. Caso
algum bairro não intercepte diretamente um distrito (por imperfeições
geométricas ou bordas), o distrito mais próximo é atribuído usando
\code{sf::st_nearest_feature}.
}
\details{
Antes da operação espacial, a função garante que ambos os objetos
estejam no mesmo sistema de referência de coordenadas (CRS).

O procedimento segue três etapas principais:
\enumerate{
\item Garantir que os objetos espaciais estejam no mesmo CRS.
\item Calcular os centróides dos bairros e realizar a interseção com
os polígonos de distritos.
\item Para bairros sem interseção direta, atribuir o distrito mais
próximo usando \code{sf::st_nearest_feature}.
}

Esse procedimento reduz problemas comuns de topologia em bases
espaciais administrativas.
}
\examples{
\dontrun{
res_distritos_nomes <- classificar_bairros_distrito(
  sp_distritos = jv_distritos_shp,
  sp_bairros = jv_shp
)

res_distritos_nomes <- res_distritos_nomes |>
  dplyr::mutate(
    nome_bairr = normalizar_bairro(nome_bairr)
  )
}

}
