% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_maps.R
\name{generate_maps}
\alias{generate_maps}
\title{Gerar mapas semanais de incidência epidemiológica}
\usage{
generate_maps(
  data,
  weeks_2_plotdistrito_nome,
  lim_range = NULL,
  value_col = "p_inc100k",
  arbo = arbo,
  geom_col = "geometry",
  shape = NULL,
  area = NULL
)
}
\arguments{
\item{data}{Data frame ou objeto \code{sf} contendo os dados espaciais e
epidemiológicos. Deve incluir as colunas \code{sem_not}, \code{arbo} e a variável
especificada em \code{value_col}.}

\item{lim_range}{Vetor numérico de comprimento 2 definindo os limites
mínimo e máximo da escala de cores. Se \code{NULL}, os limites são calculados
automaticamente a partir dos dados.}

\item{value_col}{String com o nome da coluna que contém o valor a ser
representado no mapa (ex.: incidência ou proporção).}

\item{arbo}{String indicando o arbovírus a ser filtrado
(ex.: \code{"dengue"} ou \code{"chikon"}).}

\item{geom_col}{String com o nome da coluna de geometria utilizada
para gerar os mapas.}

\item{shape}{Objeto \code{sf} opcional contendo limites administrativos
adicionais a serem desenhados sobre o mapa (ex.: regiões ou distritos).}

\item{area}{String opcional indicando a coluna em \code{shape} usada para
diferenciar áreas por tipo de linha (\code{linetype}).}

\item{weeks_2_plot}{Vetor numérico contendo as semanas epidemiológicas
que devem ser representadas nos mapas.}
}
\value{
Uma lista de objetos \code{ggplot}, onde cada elemento corresponde a um
mapa de incidência para uma semana epidemiológica.
}
\description{
Cria uma lista de mapas (\code{ggplot}) para diferentes semanas epidemiológicas,
representando indicadores de incidência espacial (ex.: casos por 100 mil
habitantes). Cada mapa corresponde a uma semana e pode incluir camadas
adicionais de limites administrativos (ex.: distritos ou regiões).
}
\details{
A função permite definir automaticamente o intervalo da escala de cores
com base nos dados disponíveis para as semanas selecionadas.

A função:
\itemize{
\item Filtra os dados para o arbovírus especificado.
\item Gera um mapa por semana epidemiológica usando \code{purrr::map}.
\item Aplica uma escala contínua de cores (\code{scale_fill_distiller}).
\item Opcionalmente adiciona limites administrativos usando \code{geom_sf}.
}

Os mapas gerados podem ser posteriormente organizados em painéis
utilizando funções auxiliares como \code{create_map_panel()}.
}
\examples{
\dontrun{
generate_maps(
  data = df_distritos,
  weeks_2_plot = c(202401, 202402, 202403),
  value_col = "inc",
  arbo = "dengue"
)

generate_maps(
  data = df_bairros,
  weeks_2_plot = c(202401, 202402, 202403),
  value_col = "inc",
  arbo = "dengue",
  shape = df_rpas,
  area = "rpa_nome"
)
}

}
