% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_rt_plot.R
\name{create_rt_plot}
\alias{create_rt_plot}
\title{Criar gráficos da série temporal do número reprodutivo efetivo (Rt)}
\usage{
create_rt_plot(
  api_data,
  weeks_limit,
  title_suffix,
  facet_by = NULL,
  lwr = NULL,
  upr = NULL,
  sem_not = NULL
)
}
\arguments{
\item{api_data}{Data frame contendo os dados do Rt. Deve incluir ao
menos as colunas \code{Rt} e a coluna de semana epidemiológica definida
em \code{sem_not}.}

\item{weeks_limit}{Última semana epidemiológica considerada na
visualização. Observações posteriores são removidas.}

\item{title_suffix}{String com o título do gráfico.}

\item{facet_by}{String opcional com o nome da coluna utilizada para
dividir os gráficos em múltiplos painéis (ex.: distrito ou bairro).
Se \code{NULL}, apenas um gráfico agregado será gerado.}

\item{lwr}{String opcional com o nome da coluna contendo o limite
inferior do intervalo de confiança do Rt.}

\item{upr}{String opcional com o nome da coluna contendo o limite
superior do intervalo de confiança do Rt.}

\item{sem_not}{String com o nome da coluna que representa a semana
epidemiológica. Default é \code{"sem_not"}.}
}
\value{
Uma lista contendo:
\itemize{
\item \code{combined_plot} — gráfico único ou facetado com todos os dados
\item \code{individual_plots} — lista de gráficos individuais para cada nível
de \code{facet_by} (ou apenas um gráfico caso \code{facet_by = NULL})
}
}
\description{
Gera gráficos da evolução semanal do número reprodutivo efetivo (Rt)
a partir de dados epidemiológicos. A função pode produzir um único
gráfico agregado ou múltiplos gráficos por grupo (facets), além de
retornar gráficos individuais para cada nível do agrupamento.
}
\details{
Opcionalmente, a função também pode incluir intervalos de confiança
para o Rt utilizando \code{geom_ribbon}.

O gráfico inclui:
\itemize{
\item Série temporal do Rt
\item Linha horizontal indicando o limiar epidêmico (\code{Rt = 1})
\item Intervalo de confiança opcional do Rt (quando \code{lwr} e \code{upr}
são fornecidos)
}

Quando \code{facet_by} é especificado, a função gera:
\itemize{
\item Um gráfico combinado com \code{facet_wrap}
\item Gráficos individuais separados para cada nível do agrupamento
}
}
\examples{
\dontrun{
create_rt_plot(
  api_data = dados_rt,
  weeks_limit = 202452,
  title_suffix = "Número Reprodutivo Efetivo (Rt)"
)

create_rt_plot(
  api_data = dados_rt,
  weeks_limit = 202452,
  title_suffix = "Rt por distrito",
  facet_by = "distrito",
  lwr = "Rt_lower",
  upr = "Rt_upper"
)
}

}
