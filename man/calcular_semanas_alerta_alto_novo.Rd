% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calcular_semanas_alerta_alto_novo.R
\name{calcular_semanas_alerta_alto_novo}
\alias{calcular_semanas_alerta_alto_novo}
\title{Contar semanas com alerta epidemiológico alto em uma janela temporal}
\usage{
calcular_semanas_alerta_alto_novo(data, semana_atual, bairro, janela)
}
\arguments{
\item{data}{Data frame contendo os dados epidemiológicos. Deve conter
ao menos as colunas \code{nm_bairro_ref}, \code{sem_not} e \code{nivel}.}

\item{semana_atual}{Semana epidemiológica de referência no formato
numérico \code{YYYYWW}.}

\item{bairro}{Nome do bairro para o qual será calculado o número
de semanas com alerta alto.}

\item{janela}{Número de semanas a considerar na janela retrospectiva.}
}
\value{
Um número inteiro representando a quantidade de semanas dentro da
janela especificada em que o bairro apresentou nível de alerta
epidemiológico alto (\code{nivel >= 3}). Caso o bairro ou a semana não
existam nos dados, a função retorna \code{0}.
}
\description{
Calcula quantas semanas recentes um bairro apresentou nível elevado
de alerta epidemiológico (níveis 3 ou 4) dentro de uma janela temporal
retrospectiva. A função percorre a série temporal do bairro até a
semana epidemiológica especificada e contabiliza quantas semanas da
janela possuem \code{nivel >= 3}.
}
\details{
O cálculo segue os seguintes passos:

\enumerate{
\item Filtra os dados para o bairro especificado
\item Ordena a série temporal por semana epidemiológica
\item Mantém apenas semanas até \code{semana_atual}
\item Seleciona as últimas \code{janela} semanas disponíveis
\item Conta quantas possuem \code{nivel >= 3}
}

Valores \code{NA} na coluna \code{nivel} são ignorados no cálculo.
}
\examples{
\dontrun{
dados <- data.frame(
  nm_bairro_ref = rep("Centro", 10),
  sem_not = 202441:202450,
  nivel = c(1,2,3,4,2,3,1,4,3,2)
)

calcular_semanas_alerta_alto(
  data = dados,
  semana_atual = 202450,
  bairro = "Centro",
  janela = 5
)
}

}
